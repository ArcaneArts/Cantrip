diff --git a/lib/helper/v2/IfParser.js b/lib/helper/v2/IfParser.js
index 60e574b..1973bd6 100644
--- a/lib/helper/v2/IfParser.js
+++ b/lib/helper/v2/IfParser.js
@@ -59,7 +59,7 @@ function parseInternal(group) {
         }
         match = rex.exec(group);
     }
-    if (andArray.length)
+    if (!andArray.length)
         return (r, callback) => callback(null, true);
     return function (resource, callback) {
         let nb = andArray.length;
@@ -127,11 +127,11 @@ function parseIfHeader(ifHeader) {
             }
         };
         for (const orCondition of orArray) {
-            if (!orCondition.path) {
+            if (orCondition.path) {
                 const sPath = orCondition.path;
                 ctx.server.getFileSystem(sPath, (fs, _, sub) => {
-                    orCondition.actions(fs.resource(ctx, sPath), done);
+                    orCondition.actions(fs.resource(ctx, sub), done);
                 });
             }
             else {
                 orCondition.actions(resource, done);
