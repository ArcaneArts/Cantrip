diff --git a/codex-rs/app-server/tests/suite/v2/skills_list.rs b/codex-rs/app-server/tests/suite/v2/skills_list.rs
index c6880fb0e..f3cbb826d 100644
--- a/codex-rs/app-server/tests/suite/v2/skills_list.rs
+++ b/codex-rs/app-server/tests/suite/v2/skills_list.rs
@@ -152,6 +152,13 @@ async fn skills_list_disabled_bundled_skills_preserves_shared_system_skill_cache
     .await??;
     assert_eq!(data.len(), 1);
     assert_eq!(data[0].errors, Vec::new());
+    assert!(
+        data[0]
+            .skills
+            .iter()
+            .any(|skill| skill.scope == SkillScope::System && skill.name == "avoid-ai-writing"),
+        "expected Avoid AI Writing to be available as a bundled system skill"
+    );
     let system_skill_paths = data[0]
         .skills
         .iter()
diff --git a/codex-rs/skills/src/lib.rs b/codex-rs/skills/src/lib.rs
index 7b19d3278..d703ce3c4 100644
--- a/codex-rs/skills/src/lib.rs
+++ b/codex-rs/skills/src/lib.rs
@@ -200,6 +200,11 @@ mod tests {
         let mut paths: Vec<String> = items.into_iter().map(|(path, _)| path).collect();
         paths.sort_unstable();
 
+        assert!(
+            paths
+                .binary_search_by(|probe| probe.as_str().cmp("avoid-ai-writing/SKILL.md"))
+                .is_ok()
+        );
         assert!(
             paths
                 .binary_search_by(|probe| probe.as_str().cmp("skill-creator/SKILL.md"))
