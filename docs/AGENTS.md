# Documentation Change Instructions

Follow `docs/MANUAL_CHANGE_PROTOCOL.md` before making any change in this
directory.

Do not write changes directly in the Primary checkout. Author every change in
its own isolated Git worktree and branch, then deliver it through the pull
request workflow defined by the protocol.
